# ServiceCrew e2e tests

Playwright coverage for ServiceCrew. Empty as of Phase 1a (DB schema +
activator/deactivator) — that task has no UI or REST surface to test
against a browser.

The first real spec lands with the first task that ships a UI page,
shortcode, or REST controller (per `plugin-qa`'s brief and the
Verification table in `ServiceCrew-Plan-v2.md`).
